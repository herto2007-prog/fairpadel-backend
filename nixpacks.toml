[phases.setup]
nixPkgs = ['nodejs-18_x']

[phases.install]
cmds = ['npm install --legacy-peer-deps']

[phases.build]
cmds = [
  'npx prisma generate',
  'npx nest build',
  'ls -la dist/'
]

[start]
cmd = 'npx prisma migrate deploy && node dist/main.js'
